To install dependencies:

```sh
bun install
```

To run:

1. Copy .env.example to .env
2. Fill the env variable
3.

    ```sh
    bun run dev
    ```

Using docker:

```sh
docker compose up
```

open <http://localhost:3000>
